// 引入路由和vue
import Vue from 'vue';
import Router from 'vue-router';
// 引入页面
import Home from '@v/Home';

// 安装
Vue.use(Router);

// 定义路由规则
let routes = [

]

// 实例化
export default new Router({ routes });